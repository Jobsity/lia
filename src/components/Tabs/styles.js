const styles = theme => ({
  tabPaper: {
    height: "-webkit-fill-available"
  },
  loading: {
    position: "absolute",
    top: "8em",
    left: "8em"
  },
  tabRoot: {
    minWidth: "auto",
    maxWidth: "inherit",
    fontWeight: "bold",
    backgroundColor: theme.palette.background.default,
    borderBottom: "1"
  }
});

export default styles;
