const styles = theme => ({
  loading: {
    position: "absolute",
    top: "8em",
    left: "8em"
  },

  tabRoot: {
    minWidth: "auto",
    fontWeight: "bold",
    backgroundColor: theme.palette.background.default,
    borderBottom: "1"
  },

});

export default styles;
