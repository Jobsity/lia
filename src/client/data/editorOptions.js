/**
 * @description This imports the necessary code to handle JavaScript on CodeMirror.
 * If you want to change the language the editor supports then modify this import's path
 * and also modify the 'mode' option in the object below.
 */
import 'codemirror/mode/javascript/javascript';

export default {
  mode: 'javascript',
  theme: 'material',
  lineNumbers: true
};
